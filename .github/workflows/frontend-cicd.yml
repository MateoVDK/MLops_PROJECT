name: Frontend CI/CD

on:
  push:
    branches:
      - main
      - master
    paths:
      - "frontend/**"
      - "k8s/frontend-deployment.yaml"
      - "k8s/frontend-service.yaml"
      - "k8s/namespace.yaml"
      - ".github/workflows/frontend-cicd.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: ${{ vars.GCP_REGION || 'europe-west4' }}
  GKE_CLUSTER: ${{ secrets.GKE_CLUSTER || vars.GKE_CLUSTER }}
  GKE_LOCATION: ${{ vars.GKE_LOCATION || vars.GCP_REGION || 'europe-west4' }}
  ARTIFACT_REPOSITORY: ${{ vars.GAR_REPOSITORY || 'blackjack' }}
  K8S_NAMESPACE: ${{ vars.K8S_NAMESPACE || 'blackjack' }}
  FRONTEND_IMAGE_NAME: blackjack-frontend

jobs:
  build-push-deploy-frontend:
    name: Build, push and deploy frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check required GitHub secrets
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          GKE_CLUSTER_VALUE: ${{ secrets.GKE_CLUSTER || vars.GKE_CLUSTER }}
        run: |
          set -euo pipefail

          if [ -z "$GCP_PROJECT_ID" ]; then
            echo "Missing GitHub secret: GCP_PROJECT_ID"
            exit 1
          fi

          if [ -z "$GCP_SA_KEY" ]; then
            echo "Missing GitHub secret: GCP_SA_KEY"
            exit 1
          fi

          if [ -z "$GKE_CLUSTER_VALUE" ]; then
            echo "Missing GitHub secret or variable: GKE_CLUSTER"
            exit 1
          fi

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
          install_components: gke-gcloud-auth-plugin

      - name: Verify GKE auth plugin
        run: |
          gke-gcloud-auth-plugin --version

      - name: Configure Docker for Artifact Registry
        run: |
          set -euo pipefail
          gcloud auth configure-docker "${REGION}-docker.pkg.dev" --quiet

      - name: Set frontend image tags
        run: |
          set -euo pipefail

          IMAGE_BASE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REPOSITORY}/${FRONTEND_IMAGE_NAME}"

          echo "FRONTEND_IMAGE_SHA=${IMAGE_BASE}:${GITHUB_SHA}" >> "${GITHUB_ENV}"
          echo "FRONTEND_IMAGE_LATEST=${IMAGE_BASE}:latest" >> "${GITHUB_ENV}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/dockerfile
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE_SHA }}
            ${{ env.FRONTEND_IMAGE_LATEST }}

      - name: Get GKE credentials
        run: |
          set -euo pipefail

          gcloud container clusters get-credentials "${GKE_CLUSTER}" \
            --location="${GKE_LOCATION}" \
            --project="${PROJECT_ID}"

      - name: Apply frontend Kubernetes manifests
        run: |
          set -euo pipefail

          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/frontend-service.yaml
          kubectl apply -f k8s/frontend-deployment.yaml

      - name: Update frontend deployment image
        run: |
          set -euo pipefail

          kubectl set image deployment/frontend frontend="${FRONTEND_IMAGE_SHA}" -n "${K8S_NAMESPACE}"
          kubectl rollout status deployment/frontend -n "${K8S_NAMESPACE}" --timeout=600s
