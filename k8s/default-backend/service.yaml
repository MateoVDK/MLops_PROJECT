apiVersion: v1
kind: Service
metadata:
  name: default-backend-service
  namespace: blackjack
spec:
  selector:
    app: default-backend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
